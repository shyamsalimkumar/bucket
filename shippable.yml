# language setting
language: node_js
# version numbers, testing against versions of node
node_js:
  - 0.10.24
  - 0.8.14
before_install:
  - npm install -g mocha

# git config
git:
 submodules: false
# branches to build off
branches:
  only:
    - master